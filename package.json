{
  "name": "tax-transparency-tracker",
  "version": "1.0.0",
  "description": "AI-powered civic web app showing Indian citizens where their taxes go",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "test": "npm --prefix backend test && npm --prefix frontend test && node scripts/run-ml-tests.js",
    "coverage": "npm --prefix backend run coverage && npm --prefix frontend run coverage && node scripts/run-ml-tests.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
